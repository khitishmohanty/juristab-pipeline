# Use an official Python runtime as a parent image
FROM python:3.9-slim

# Set the working directory in the container
WORKDIR /app

# Copy the requirements file and install dependencies
# This is done first to leverage Docker's layer caching
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy the rest of the application's code into the container
COPY . .

# The port that your application listens on. 
# This matches the port in your apprunner.yaml and main.py
EXPOSE 8080

# The command to run your application using Gunicorn
# This is the same command from your apprunner.yaml
CMD ["gunicorn", "--workers=1", "--threads=4", "--bind", "0.0.0.0:8080", "main:server"]